# wallust v3.4
#
# You can copy this file to ~/.config/wallust/wallust.toml (keep in mind is a sample config)

# SIMPLE TUTORIAL, or `man wallust.5`:
# https://explosion-mental.codeberg.page/wallust/
#
# If comming from v2: https://explosion-mental.codeberg.page/wallust/v3.html#wallusttoml

# Global section - values below can be overwritten by command line flags

# How the image is parse, in order to get the colors:
# full - resized - wal - thumb -  fastresize - kmeans
backend = 'kmeans'

# What color space to use to produce and select the most prominent colors:
# lab - labmixed - lch - lchmixed
color_space = 'labmixed'

# Use the most prominent colors in a way that makes sense, a scheme color palette:
# dark - dark16 - darkcomp - darkcomp16
# light - light16 - lightcomp - lightcomp16
# harddark - harddark16 - harddarkcomp - harddarkcomp16
# softdark - softdark16 - softdarkcomp - softdarkcomp16
# softlight - softlight16 - softlightcomp - softlightcomp16
palette = 'dark16'

# Ensures a 'readable contrast' (OPTIONAL, disabled by default)
# Should only be enabled when you notice an unreadable contrast frequently happening
# with your images. The reference color for the contrast is the background color.
#check_contrast = true

# Color saturation, between [1% and 100%] (OPTIONAL, disabled by default)
# usually something higher than 50 increases the saturation and below
# decreases it (on a scheme with strong and vivid colors)
#saturation = 35

# Alpha value for templating, by default 100 (no other use whatsoever)
#alpha = 100


# Difference between similar colors, used by the colorspace:
#  1          Not perceptible by human eyes.
#  1 - 2      Perceptible through close observation.
#  2 - 10     Perceptible at a glance.
#  11 - 49    Colors are more similar than opposite
#  100        Colors are exact opposite
threshold = 11

[templates]
# NOTE: prefer '' over '' for paths, avoids escaping.
# template: A RELATIVE path that points to `~/.config/wallust/template` (depends on platform)
# target: ABSOLUTE path in which to place a file with generated templated values.
# ¡ If either one is a directory, then both SHOULD be one. !
# zathura = { template = 'zathura', target = '~/.config/zathura/zathurarc' }

hypr.template = 'hyprland-colors.conf'
hypr.target = '~/.config/hypr/active-colors.conf'

rofi.template = 'colors-rofi.rasi'
rofi.target = '~/.config/rofi/themes/colors.rasi'

btop.template = 'btop.theme'
btop.target = '~/.config/btop/themes/active.theme' 

cava.template = 'cava-colors.ini'
cava.target = '~/.config/cava/colors.ini'

alacritty.template = 'alacritty-colors.toml' 
alacritty.target = '~/.config/alacritty/colors.toml'

starship.template = 'starship-colors.toml'
starship.target = '~/.config/starship/colors.toml' 

tsumiki.template = 'tsumiki.scss'
tsumiki.target = '~/.config/tsumiki/styles/themes/active.scss'

foot.template = 'foot.ini'
foot.target = '~/.config/foot/colors.ini' 

yazi.template = 'yazi-theme.toml'
yazi.target = '~/.config/yazi/colors.toml'

kitty.template = 'kitty-colors.conf'
kitty.target = '~/.config/kitty/colors.conf'

gtk3.template = 'gtk-colors.css'
gtk3.target = '~/.config/gtk-3.0/colors.css'

gtk4.template = 'gtk-colors.css'
gtk4.target = '~/.config/gtk-4.0/colors.css'

pywalfox.template = 'pywalfox-colors.json'
pywalfox.target = '~/.cache/wal/colors.json'

micro.template = 'micro.micro'
micro.target = '~/.config/micro/colorschemes/active.micro'

css.template = "colors.css"
css.target = '~/.config/wallust/cache/colors.css'

# foot.template = 'foot.ini'
# foot.target = '~/.config/foot/wallust-colors.ini' 

# pywalfox.template = 'pywalfox-colors.json'
# pywalfox.target = '~/.config/wallust/cache/colors.json' 
# pywalfox post hook post_hook = 'pywalfox update'

#zathura.template = 'colors-zathura'
#zathura.target = '~/.config/zathura/zathurarc'
